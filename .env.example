# 数据库类型（目前仅支持 mysql）
DB_TYPE=mysql

# 数据库连接信息
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=auto_news

# 爬取模式
# local = 仅写入本地 JSON 文件（默认）
# online = 写入 MySQL 数据库
CRAWLER_MODE=local
